import { AddDragDirective } from './add-drag.directive';

describe('AddDragDirective', () => {
  it('should create an instance', () => {
    const directive = new AddDragDirective();
    expect(directive).toBeTruthy();
  });
});
